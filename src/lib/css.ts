export const viewer=`.main>.article {
    display: flow-root;
    padding: 0 var(--length-padding);
    margin: 0 auto;
    max-width: var(--length-width);
    min-width: 100px;
    margin-bottom: 100vh;
}`
export const all=viewer